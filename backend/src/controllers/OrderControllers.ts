import { type Socket } from 'socket.io'
// import ProductServices from '../helpers/ProductServices'

export default class OrderControllers {
  public static createOrder (_socket: Socket, _payload: string[]): void {
    // const orderPrice = payload.map(async (id): Promise<string> => {
    //   return await ProductServices.findById(id)
    // })
  }
}
